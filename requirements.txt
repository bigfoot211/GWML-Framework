numpy>=1.24
scipy>=1.10
pandas>=1.5
scikit-learn>=1.2
matplotlib>=3.6        # 可视化（可选，演示脚本使用）
# 可选依赖（按需安装）：
# shap>=0.42           # GWRF.local_importance(method='shap')
# geopandas>=0.14      # 面矢量读取（rasterize_polygons 可选路径）
# rasterio>=1.3        # 矢量栅格化（rasterize_polygons 可选路径）
